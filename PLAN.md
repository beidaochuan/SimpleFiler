# SimpleFiler 開発計画

## 1. 製品方針

SimpleFilerは、Windows 10/11 x64向けのポータブルな2ペインファイラーとする。
タブは採用せず、左右のペイン、キーボード操作、登録先への高速ジャンプ、
登録アプリの起動に機能を集中する。

現行実装は試作として扱い、新しい操作仕様を優先する。ただし、設定、非同期列挙、
Windows Shell操作など、再利用できる処理はテストで保護して段階的に利用する。

## 2. 初版の必須機能

- 2ペインを基本とする表示（必要な場合は1ペインへ切り替え可能）
- 左右ペインの独立した移動履歴
- キーボード中心のファイル選択とナビゲーション
- `ff...`による登録フォルダー検索とジャンプ
- `aa...`による登録アプリ検索と起動
- 選択ファイル、現在のフォルダーを登録アプリへ渡す機能
- 現在のフォルダーから通常CMD／管理者CMDを起動する機能
- コピー、移動、貼り付け、名前変更、新規フォルダー
- Windows標準の削除操作
- SimpleFilerフォルダー内への設定保存

## 3. 2ペイン操作

2ペイン表示を既定とし、左右どちらか一方を常にアクティブペインとして
色とフォーカス枠で明示する。
コピーと移動の既定の宛先は反対側のペインとする。

| キー | 操作 |
| --- | --- |
| `Tab` | アクティブペイン切り替え |
| `Enter` | ファイルを開く／フォルダーへ移動 |
| `Backspace` | 親フォルダーへ移動 |
| `Alt+Left` | 戻る |
| `Alt+Right` | 進む |
| `Ctrl+L` | アドレス入力 |
| `Ctrl+R` | 更新 |
| `F2` | 名前変更 |
| `F5` | 反対ペインへコピー |
| `F6` | 反対ペインへ移動 |
| `Ctrl+N` / `F7` | 新規フォルダー |

### 削除キー

削除はWindows標準のキーだけを使用し、独自ショートカットは追加しない。

- `Delete`: ごみ箱へ移動
- `Shift+Delete`: 完全削除
- `F8`などの独自削除キーは割り当てない
- 完全削除ではWindows標準の確認と警告を表示する

## 4. `ff`フォルダージャンプ

ファイル一覧にフォーカスがある状態から文字入力を開始できる。
`ff`を入力すると登録フォルダー候補を表示し、続く文字でリアルタイムに絞り込む。

`ffwork`と`ff work`は同じ検索として扱う。検索対象は表示名、パス、エイリアス、
キーワードとし、大文字・小文字は区別しない。

| キー | 操作 |
| --- | --- |
| `Up` / `Down` | 候補選択 |
| `Enter` | アクティブペインで開く |
| `Shift+Enter` | 反対側のペインで開く |
| `Ctrl+N` | 現在のフォルダーを新規登録 |
| `Escape` | 候補を閉じる |

`ff`候補画面または登録管理画面から、現在のフォルダーを登録、編集、削除できるようにする。

## 5. `aa`アプリランチャー

`aa`を入力すると登録アプリ候補を表示し、続く文字で絞り込む。
表示名、実行ファイル、エイリアス、キーワードを検索対象とする。

| キー | 操作 |
| --- | --- |
| `Enter` | 通常起動 |
| `Ctrl+Enter` | 管理者として起動 |
| `Shift+Enter` | 選択ファイルを渡さず起動 |
| `Ctrl+N` | アプリを新規登録 |
| `Escape` | 候補を閉じる |

アプリ登録には表示名、実行ファイル、引数テンプレート、作業フォルダー、
キーワード、管理者起動の既定値を保持する。

引数テンプレートでは、少なくとも次を利用できるようにする。

- `{file}`: 最初の選択ファイル
- `{files}`: 選択した全ファイル
- `{folder}`: アクティブペインのフォルダー
- `{left}`: 左ペインのフォルダー
- `{right}`: 右ペインのフォルダー
- `{other}`: 反対ペインのフォルダー

## 6. CMD

次のコマンドを候補パレットから実行できるようにする。

- `cmd`: アクティブペインを作業フォルダーにしてCMDを起動
- `cmd admin`: 同じフォルダーから管理者CMDを起動

ローカルパス、UNC、空白、日本語、CMDで意味を持つ記号を含むパスに対応する。
SimpleFiler本体は通常権限のまま動作し、管理者CMDだけを`runas`で起動する。
UACをキャンセルしてもSimpleFilerは継続する。

## 7. ポータブル設定

設定は`SimpleFiler.exe`と同じSimpleFilerフォルダーに保存し、
`AppData`など別の場所へ自動的に移さない。

```text
SimpleFiler/
  SimpleFiler.exe
  simplefiler.json
```

- 一時ファイルへ書いた後に原子的に置き換える
- 破損設定は退避して既定値で起動する
- 未知の設定項目は保存時にも維持する
- SimpleFilerフォルダー内の登録アプリは可能な限り相対パスで保持する
- 書き込み不可の場合は状態を明示し、別の保存先へ勝手にフォールバックしない

## 8. 内部設計

UIと機能ロジックを分離する。

```text
Application
  PaneController
  CommandPalette
    FolderCommandProvider
    AppCommandProvider
    TerminalCommandProvider
  FileSystemService
  FileOperationService
  ProcessLauncher
  SettingsStore
  Windows UI
```

- コマンド解析、候補順位、設定変換はUI非依存にする
- ファイル列挙は非同期、キャンセル可能にする
- 古い列挙結果は世代番号で破棄する
- UIスレッドではディスク処理を行わない
- 管理者権限をSimpleFiler本体へ昇格させない

## 9. 自動テスト

### 単体テスト

- `ff`、`aa`、`cmd`の解析
- 空白あり／なし、日本語、大文字・小文字
- 候補の完全一致、前方一致、部分一致、あいまい一致の順位
- アプリ引数テンプレート展開と引用符
- CMDのローカル／UNC起動パラメーター
- 設定の保存、復元、破損、値補正、未知項目維持
- ポータブル相対パス
- `Delete`／`Shift+Delete`以外に削除キーが登録されていないこと

### Windows統合テスト

- 一時フォルダーの列挙、Unicode名、隠し属性
- 検索とキャンセル
- コピー、移動、名前変更、新規フォルダー
- 通常削除と完全削除
- 長いパスとアクセス拒否
- 設定フォルダー全体の移動

### 実機テスト

- UAC表示とキャンセル
- 管理者CMD
- ごみ箱
- ファイル競合ダイアログ
- 高DPI、IME、キーボードフォーカス
- Windows 10/11

## 10. 性能・安全性目標

- 通常起動は基準PCで1秒以内
- 1万項目のフォルダーでもUIを停止させない
- `ff`／`aa`候補更新は50ミリ秒以内
- コピー・移動中もUIを操作可能にする
- 完全削除は明確に区別し、誤操作を避ける
- 失敗時に元ファイルを失わない

## 11. 実装順序

1. 本文書を操作仕様と受け入れ条件の基準にする
2. 標準削除キーとポータブル設定の扱いを固定する
3. UI非依存のコマンド解析・候補順位付けを実装してテストする
4. 共通コマンドパレットUIを実装する
5. `ff`フォルダージャンプを接続する
6. `aa`アプリランチャーを接続する
7. 通常／管理者CMDを接続する
8. `Tab`、`F5`、`F6`、`F7`を含む2ペイン操作を完成させる
9. 自動テストとWindows実機テストを拡充する
10. ポータブルReleaseパッケージを作成する

## 12. 初版の完成条件

- 左右2ペインをキーボードだけで操作できる
- `ff...`から登録フォルダーへ移動できる
- `aa...`から登録アプリを起動できる
- 選択ファイルまたは現在のフォルダーを登録アプリへ渡せる
- 現在のフォルダーから通常／管理者CMDを起動できる
- `Delete`／`Shift+Delete`がWindows標準どおり動作する
- 独自の削除ショートカットが存在しない
- 設定がSimpleFilerフォルダー内だけに保存される
- 主要な単体・統合テストが成功する
- 1万項目でもUIが応答し続ける

## 13. 現在の実装状況

初版の機能実装は完了している。`ff`／`aa`／`cmd`の共通候補パレット、
2ペイン操作、標準削除キー、アプリ引数テンプレート、通常／管理者端末、
ポータブル設定、ZIP、非同期ファイル操作を接続済み。

安全性として、設定の原子的保存、破損設定の退避、未知項目の維持、
書き込み不可の通知、ZIP展開パス検証、入力欄での削除キー誤作動防止、
ソート後の選択維持、処理中の終了確認を実装した。Per-Monitor V2 DPIに合わせて
フォント、レイアウト、分割バー、列幅も再計算する。

自動テストは次を継続して実行する。

- コマンド解析、候補順位、設定、CMDパラメーター
- アプリ引数テンプレートとWindows引用符
- ポータブル相対パス
- 実ファイルのコピー、移動、名前変更、フォルダー作成
- Unicode、隠し属性、検索中止、再解析ポイント、1万項目の列挙
- `ff`ジャンプ、`aa`実起動、候補操作、入力欄の`Delete`を含むUIスモーク
- Release ZIPの内容、起動、EXE隣への設定保存

UAC、ごみ箱、競合ダイアログ、複数DPIモニター間移動、IME、
Windows 10/11差異はOS対話を伴うため、`docs/WINDOWS_TEST_CHECKLIST.md`で
リリース候補ごとに実機確認する。
